options:
  registry_tag:
    type: string
    default: 2.6.0
    description: Registry tag to run
  registry_port:
    type: int
    default: 5000
    description: Host port to bind the Docker registry
  install_from_upstream:
    type: boolean
    default: true
    description: Force it to workaround Server/Client API mismatches
